
;
; make-jarray
;

define (make-jarray . l)
  l

;
; make-jobject
;

define (make-jobject . l)
  let
    _
      dictionary (make-alist l)
    define (get ke)
      assocv ke dictionary
    define (alist)
      dictionary
    define (dispatch meth . args)
      apply meth args
    dispatch
