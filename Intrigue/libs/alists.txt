
;
; alist related functions
;

; move me to lists.txt?
;
define
  all? fu? li
  cond
    (empty? li) true
    (fu? (car li)) (all? fu? (cdr li))
    (true false)

;
; alist?

define
  alist? o
  if (list? o)
    all? (lambda (li) (list? li)) o
    false
