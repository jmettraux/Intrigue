
;
; gcd (Euler)
;

define
  gcd2 m n
  if (zero? n)
    abs m                   ; then
    gcd2 n (remainder m n)  ; else

define
  gcd . ns
  if (empty? ns)
    0                             ; then
    foldl gcd2 (car ns) (cdr ns)  ; else
